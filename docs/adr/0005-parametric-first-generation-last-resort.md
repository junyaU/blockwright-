# パラメトリックを優先し、生成は最後の逃げ道とする

型（パラメータ）で表せるもの（box/house/tower/wall/bridge）は**決定論の専用生成器**で作り、AIに形を生成させる経路には流さない。生成（非決定論）は**パラメトリックで表せない自由形状（キャラクター等）のときだけ**の逃げ道とする。パラメトリックは崩れず正確、生成は荒く非決定論であり、安定を捨てる理由がないなら捨てないため。

## 判断軸

新機能設計時は「これはパラメータで表せるか？」を先に問う ── Yesなら専用生成器、Noではじめて生成。家・塔・壁を生成経路に流すのはアンチパターン。
