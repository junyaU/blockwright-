# 自由形状の器 grid IR を密版で定義する

v2.x で自由形状を表す `GridIR` を、`voxels[y][z][x]` の**密（dense）配列**として定義する。疎（sparse）表現は採らない。疎は密で表せるが逆は不可であり、器（ターゲット表現）を間違えると、後から充填手段を作る段で器ごと作り直しになるため。「この器は、まだ見ぬ充填手段に耐えるか？」を先に問い、耐える側を選んだ。

## 帰結

- v3 のボクセル化（充填エンジン）は、この器に充填を差すだけで済んだ。
- v4/v5 のキャラクターもすべて GridIR に合流し、単一の合流点になっている。
