# IRは絶対座標を持たず、配置は origin で外から与える

IRは「**何を**建てるか」だけを表し、「**どこに**建てるか」は `build(ir, origin)` の `origin` 引数として外から与える。配置と形状を直交させ、同じIRを任意の場所に建てられるようにするため。

## 帰結

- v5 の「移動」が `origin` を差し替えるだけで済み、形を一切触らずに位置だけ動かせる。
- IRに絶対座標を持ち込むことは禁止（[0001](0001-fixed-ir-seam.md) の不変条件に連なる）。
